diff a/crates/paws_domain/src/env.rs b/crates/paws_domain/src/env.rs	(rejected hunks)
@@ -298,6 +302,7 @@ fn test_command_path() {
         workspace_server_url: "http://localhost:8080".parse().unwrap(),
         override_model: None,
         override_provider: None,
+        enable_permissions: false,
     };
 
     let actual = fixture.command_path();
@@ -337,6 +342,7 @@ fn test_command_cwd_path() {
         workspace_server_url: "http://localhost:8080".parse().unwrap(),
         override_model: None,
         override_provider: None,
+        enable_permissions: false,
     };
 
     let actual = fixture.command_cwd_path();
@@ -376,6 +382,7 @@ fn test_command_cwd_path_independent_from_command_path() {
         workspace_server_url: "http://localhost:8080".parse().unwrap(),
         override_model: None,
         override_provider: None,
+        enable_permissions: false,
     };
 
     let command_path = fixture.command_path();
